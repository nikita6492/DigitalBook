package com.digitalbooks.bookservice.repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import com.digitalbooks.bookservice.entity.Book;

@Repository
public interface BookRepository extends JpaRepository<Book, Long> {
	@Query("SELECT * FROM Book b where (b.category=?1 OR b.title=?2 OR b.price=?3 OR b.publisher=?4 OR b.author=?5) AND status='unblock'")
	public List<Book> findBySearchCriteria(String category,String title,Double price,String publisher,String author);

}
